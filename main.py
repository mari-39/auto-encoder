import numpy as np
import tqdm as tqdm
import matplotlib as mpl
import utils
from auto_encoder import AutoEncoder as AE

# use train on training.csv (included in auto_encoder)
# use W1, ..., W4 on test.csv
